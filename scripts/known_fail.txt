# Known-failing fixtures — sweep exits 0 if the failing set is a subset
# of this allowlist, exits 1 if any UNEXPECTED failure appears OR if a
# known-failing fixture starts passing (that would mean this list needs
# updating and should be reviewed).
#
# One fixture name per line.  Blank lines and `#` comments ignored.

# (empty — `relu` was removed 2026-04-27 after Bug 6 fix landed.
#  The FG26 + descriptor UR4 collision that produced relu's sync=700
#  was resolved at sass/isel.py:1421 in commit 025599a62c. Subsequent
#  loop-optimization passes confirmed relu now passes on GPU; the
#  corpus-gpu run at workflow ID 25029383142 surfaced it as an
#  UNEXPECTED-PASS, prompting this delisting.)
